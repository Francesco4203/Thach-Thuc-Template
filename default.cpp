#include <bits/stdc++.h>
using namespace std;

// #define int long long
#define x first
#define y second
#define pb push_back
#define pii pair<int, int>
#define ll long long
#define pdd pair<double, double>
#define rp(i, a, b) for (int i = (a), _b = (b); i < _b; ++i)

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    int test = 1;
    // cin >> test;
    rp(cas, 0, test) {
    }
    return 0;
}
