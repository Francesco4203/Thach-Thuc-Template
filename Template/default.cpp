#include <bits/stdc++.h>
using namespace std;

#define x first
#define y second
#define pii pair<int, int>
#define pll pair<long long, long long>
#define ll long long
#define pdd pair<double, double>
#define rp(i, l, r) for (int i = l; i < r; i++)

// Global variables pls

int main()
{
    int test = 1;
    // cin >> test; if many testcases
    rp(cas, 0, test)
    {
        // solution
    }
    return 0;
}